# PROJECT_LOG

## 2026-06-21：补充需求代码映射与项目文件说明

- 完成内容：新增需求到代码的逐项映射文档，覆盖资格条件、标注、训练、GUI、量化、Python/C++ 推理和 SQLite，并给出关键调用链与当前实现边界；新增项目文件说明文档，解释全部根目录源码、Python 桥接、构建配置、模型、第三方依赖、数据集、运行产物和协作目录的用途。
- 修改文件或模块：新增 `REQUIREMENTS_CODE_MAP.md`、`PROJECT_FILES.md`，更新 `PROJECT_LOG.md`。
- 修改原因：需要让需求表中的每一项都能快速定位到当前代码入口，同时让维护者理解项目文件职责，避免误把空的 `Annotation.h` 或辅助 `mainwindow.ui` 当作主要实现来源。
- 修改效果：可以从需求反查类、函数、脚本和数据库表，也可以从文件名正向理解模块边界、输入输出和依赖关系；文档明确记录了 QAT、独立数据集管理页和 C++ TensorRT 等当前边界。
- 风险、限制与后续：文档使用稳定的函数名而非易漂移行号；若后续重命名类、调整数据库表或增加文件，需要同步维护两份说明。数据集和构建目录按目录级说明，没有逐一列出大量图片、标签和生成文件。
- 验证：只读盘点项目根目录，核对 CMake 源文件清单、C++ 类/函数声明与实现、两个 Python 桥接脚本及现有数据库初始化 SQL；检查两份 Markdown 均覆盖用户要求。此次仅新增文档，没有修改可执行代码，因此未重新构建程序。

## 2026-06-21：恢复训练性能与模型信息并支持选择结果目录

- 完成内容：在训练页恢复独立的训练结果摘要表，显示训练结果目录、`weights/best.pt`、`weights/last.pt`，并读取 `results.csv` 最后一轮的 epoch、Precision、Recall、mAP50、mAP50-95 及训练/验证 loss；在结果图浏览器增加“选择结果目录”按钮，选择历史目录后同步刷新摘要和图片。
- 修改文件或模块：`mainwindow.h`、`mainwindow.cpp`、`README.md`、`PROJECT_LOG.md`。
- 修改原因：上一项改造用结果图片浏览器替换实时指标区时，性能指标和模型文件信息失去了界面入口；同时结果图只能自动选择最近目录，无法主动查看其他训练批次。
- 修改效果：当前训练完成后会自动展示模型文件与最终指标，已有训练目录也可直接选择查看；图片、指标和模型信息始终对应同一个结果目录。
- 风险、限制与后续：指标解析面向 Ultralytics 标准逗号分隔 `results.csv`；若第三方训练器使用带引号逗号字段或不同文件名，需要增加专用 CSV 解析适配。模型文件按标准 `weights/best.pt`、`weights/last.pt` 查找，缺失时显示“未生成”。
- 验证：Qt 6.11 / MSVC 2022 下编译和链接达到 100%，offscreen 启动后持续运行 3 秒且未崩溃；只读核对 `train_20260618_104913` 最后一轮为 epoch 100、Precision 0.88156、Recall 0.82313、mAP50 0.89134、mAP50-95 0.67179，且 `best.pt`、`last.pt` 均存在。未执行真实训练；目录选择对话框和界面布局需人工可见 GUI 验收。

## 2026-06-21：重新标注与训练结果图浏览

- 完成内容：将标注区“重做”重新定义为清空当前图片的全部标注框，执行前显示数量与确认提示，清空动作可通过“撤销”恢复；将训练指标表和实时曲线区域替换为训练结果图片浏览器，支持上一张/下一张、文件名及页码显示，训练完成后按桥接消息中的 `save_dir` 自动加载，程序启动及切换输出目录时自动加载最近的 `train_*` 目录。
- 修改文件或模块：`canvaswidget.cpp`、`mainwindow.h`、`mainwindow.cpp`、`README.md`、`PROJECT_LOG.md`。
- 修改原因：项目中的“重做”业务含义是清除现有框后重新标注，并非恢复撤销操作；训练结果更适合直接查看 Ultralytics 输出的 `results.png`、混淆矩阵、PR 曲线和批次样例，而不是在界面内重复绘制有限的实时指标。
- 修改效果：当前图片存在框时可一键清空并重新绘制，误操作仍可撤销；已有 `train_20260618_104913` 等训练目录无需重新训练即可在训练页分页浏览其中的常见图片格式。
- 风险、限制与后续：结果浏览器当前读取训练目录顶层的 PNG/JPG/JPEG/BMP/WEBP 文件并按名称排序，不递归读取子目录；epoch 指标仍写入 SQLite，但训练页不再显示实时指标曲线；GUI 图片切换和确认框仍需人工交互验收。
- 验证：Qt 6.11 / MSVC 2022 下 `YOLOAnnotator` 编译、链接达到 100%；使用 offscreen 平台启动程序并保持运行 3 秒，未发生启动崩溃；只读核对 `runs/train_20260618_104913` 顶层可浏览结果图共 20 张。未启动真实训练，避免占用训练资源；确认框、清空后撤销以及图片按钮切换仍需在可见 GUI 中人工验收。

## 2026-06-21：推理检测记录点击高亮

- 完成内容：在推理结果区域增加当前图片检测框表格，显示类别、置信度和坐标；点击任意记录后，右侧结果图使用青色粗框、半透明填充和标签实时高亮对应检测框；翻页时自动加载当前图片记录并清除上一张高亮。
- 修改文件或模块：`quantdialog.h`、`quantdialog.cpp`、`README.md`、`PROJECT_LOG.md`。
- 修改原因：原结果图片将全部框固定绘制为红色，用户无法从数据库明细快速定位某一条检测记录对应的目标。
- 修改效果：Python 与 C++ 推理任务均可按 `task_id + image_name` 关联图片和检测记录；高亮只绘制在内存中的展示副本上，不覆盖磁盘结果图片。
- 风险、限制与后续：当前数据库只保存图片文件名而非完整来源路径，同一推理任务若包含不同目录下的同名图片，记录可能无法唯一匹配；现有单目录/单图流程不受影响。旧错误推理记录仍保留。
- 验证：ONNX Runtime 启用模式下 Qt 6.11 / MSVC 2022 编译、链接和部署通过，`YOLOAnnotator` 目标达到 100%；核对 Python 桥接与 C++ 后端均以结果文件名写入 `detections.image_name`，与展示区按当前结果文件名查询的关联条件一致。未执行自动化鼠标点击测试，原因是项目暂无 Qt GUI 交互测试框架；需启动最新程序完成一次推理后人工点击记录确认颜色反馈。

## 2026-06-21：修复 YOLO26 ONNX C++ 端到端输出解析

- 完成内容：ONNX 模型加载时读取 `end2end` 和 `names` 元数据；新增 `[1,N,6]` 输出分支，按 `[x1,y1,x2,y2,confidence,class_id]` 解码、反算 letterbox 坐标、裁剪图片边界并执行 NMS；传统 `[1,C,N]` 原始 YOLO 输出解析保持不变。
- 修改文件或模块：`onnxinferenceengine.cpp`、`README.md`、`PROJECT_LOG.md`。
- 修改原因：YOLO26n 导出的 `best.onnx` 是端到端模型，现有代码错误地把 xyxy 坐标当作 cxcywh，并把置信度和类别 ID 当作类别分数，造成大量乱框。
- 修改效果：当前模型的第一条原始检测 `[194.0951,211.9704,563.5555,482.9366,0.8074,0]` 会正确映射回原图约 `[151.64,84.35,440.28,296.04]`，类别读取为 `motorbike`，不再生成跨屏乱框和 `1.00` 错误标签。
- 风险、限制与后续：六列输出通过模型元数据和张量布局共同识别；自定义六列但语义不同的第三方模型仍需单独适配。此前错误 C++ 推理产生的结果图片和数据库检测记录未删除，需要用户确认后才能清理。
- 验证：ONNX Runtime 启用模式下 Qt 6.11 / MSVC 2022 构建、链接和 DLL 部署通过，`YOLOAnnotator` 目标达到 100%。使用现有模型和 `009573.jpg` 核对 letterbox 反算：模型坐标 `[194.0951,211.9704,563.5555,482.9366]`、缩放比 `1.28`、顶部填充 `104`，还原为原图约 `[151.64,84.35,440.28,296.04]`，与 Python 推理数据库中的坐标一致。未自动删除旧错误结果，也未替用户启动 GUI 执行新推理。

## 2026-06-21：定位 ONNX Runtime C++ 推理乱框原因

- 完成内容：以只读方式检查实际 `best.onnx` 的输入输出、模型元数据和一张样例图片的原始推理张量，并对照 `onnxinferenceengine.cpp` 的后处理实现定位问题。
- 涉及文件或模块：未修改推理代码、模型或数据库；仅更新 `PROJECT_LOG.md`。
- 原因：该 YOLO26n 模型输出形状为 `[1, 300, 6]`，元数据标记 `end2end=True`，每行格式是 `[x1, y1, x2, y2, confidence, class_id]`；现有 C++ 后处理仅按传统原始 YOLO 输出 `[cx, cy, w, h, class_scores...]` 解析，导致右下角坐标被误当宽高、类别 ID 被误当分数。
- 效果：已确认乱框不是 ONNX Runtime 数值推理失败，也不是 RGB、缩放或 DLL 配置问题，而是输出格式适配缺失；修复方向是增加端到端 6 列输出分支，并从模型元数据读取 `end2end` 和类别名称。
- 风险、限制与后续：当前 C++ 推理产生的结果图片及检测数据库记录不可信，但本次未删除或覆盖这些数据。后续修复应同时保留传统 `[1, C, N]` 原始输出解析，不能只针对当前模型硬编码。
- 验证：ONNX Runtime Python CPU 后端读取到输入 `[1,3,640,640]`、输出 `[1,300,6]`；样例 `009573.jpg` 仅有 1 条置信度大于 0.25 的正确检测，首条为 `[194.0951,211.9704,563.5555,482.9366,0.8074,0]`，与 Python 推理结果一致。

## 2026-06-21：完成 7.1 ONNX Runtime 配置

- 完成内容：从 Microsoft 官方 GitHub Release 下载并安装 ONNX Runtime 1.26.0 CPU / Windows x64 C/C++ SDK；将 SDK 完整解压到 `third_party/onnxruntime-win-x64-1.26.0`；为当前 Qt 6.11 / MSVC 2022 构建目录设置 `ONNXRUNTIME_ROOT`，启用原生 C++ 推理编译，并自动部署运行时 DLL。
- 修改文件或模块：新增 `third_party/onnxruntime-win-x64-1.26.0/` 官方 SDK；更新当前构建目录 CMake 缓存、`README.md` 和 `PROJECT_LOG.md`。未修改 ONNX 推理接口、Qt/Python/Ultralytics 版本或依赖锁文件。
- 修改原因：此前项目仅提供可选 ONNX Runtime 探测逻辑，机器上没有 SDK，C++ 推理后端一直处于禁用状态。
- 修改效果：CMake 输出 `ONNX Runtime C++ inference enabled`；`YOLOAnnotator.exe` 已链接 `onnxruntime.lib`，输出目录已包含 `onnxruntime.dll`，程序中的 C++ / ONNX Runtime 后端现已编译启用。
- 风险与限制：当前安装的是 CPU 版，不启用 CUDA Execution Provider；SDK 路径保存在现有构建目录的 CMake 缓存中，新建或清空构建目录后需要重新设置 `ONNXRUNTIME_ROOT`。本次只完成 7.1 环境配置，具体 ONNX 模型输出形状和推理结果正确性仍需按 7.2 使用实际模型验证。
- 验证：官方压缩包包含所需头文件、导入库和 DLL；CMake 配置成功识别 SDK；Qt 6.11 / MSVC 2022 全量编译与链接完成，`YOLOAnnotator` 达到 100%；`dumpbin /dependents` 确认可执行文件依赖 `onnxruntime.dll`；使用 `ctypes.WinDLL` 成功加载输出目录 DLL 并找到 `OrtGetApiBase` API。构建仅保留已有的 Qt `QMenu::addAction` 弃用警告，无 ONNX Runtime 错误。

## 2026-06-18：核查推理数据库记录

- 完成内容：以只读方式核查程序实际使用的 SQLite 文件、数据表和最新推理记录。
- 涉及文件或模块：未修改程序代码或数据库；仅更新 `PROJECT_LOG.md`。
- 核查原因：用户在 Navicat 中未看到推理数据，需要确认实际数据库路径与入库状态。
- 核查结果：实际数据库为 `C:\Users\colin\AppData\Local\YOLO Annotator\yolo_results.db`，大小 77,824 字节，最后修改时间为 2026-06-18 13:56:49；包含 1 条 `inference_tasks` 记录和 7 条 `detections` 记录。任务 ID 为 1，Python 后端，状态为 `completed`，检测类别均为 `motorbike`。
- 风险、限制与后续：Navicat 可能连接了不带空格的错误目录，或尚未刷新表数据；`detections.timestamp` 使用 SQLite `CURRENT_TIMESTAMP`，显示为 UTC 时间，比本地时间少 8 小时，但不影响记录内容。
- 验证：使用 Python `sqlite3` 的只读 URI 模式查询表名、记录总数和最近 10 条记录成功；未对数据库执行写入、迁移或结构修改。

## 2026-06-18：扩大训练指标区域并修复曲线不显示

- 完成内容：将“数据集与运行环境”和“训练参数”改为顶部一行两列；把开始训练、停止训练和总进度移到配置区下方；限制日志区高度，并将指标表与曲线图改为可拖动的左右分栏。为 mAP 和 loss 分别建立左右纵轴，启用数据点显示和动态 loss 范围；每个 epoch 只向 loss 曲线追加一个平均值，避免同一横坐标出现多条竖线。
- 修改文件：`mainwindow.h`、`mainwindow.cpp`、`qt_train_bridge.py`、`PROJECT_LOG.md`。
- 修改原因：原纵向堆叠布局占用了大部分窗口高度；自动坐标轴在无数据和单个数据点时缺少明确反馈。同时，终端进度文本可能与 JSON epoch 消息粘在同一行，导致整行 JSON 解析失败，指标表和曲线都收不到数据。
- 修改效果：指标区域获得主要伸缩空间，表格与图表可横向调整；单个 epoch 也会显示圆点，后续 epoch 形成曲线；混在终端文本后的结构化指标仍可被识别。Python 桥接改用当前 Ultralytics 的 `trainer.tloss` 和 `label_loss_items()` 提取具名训练损失。
- 风险与限制：曲线依赖 Ultralytics 每个 epoch 触发 `on_fit_epoch_end`；若第三方版本不提供验证指标，界面只能显示实际收到的 loss 等字段。loss 曲线使用各损失项的平均值用于趋势观察，详细分项仍完整保留在指标表和数据库中。
- 验证：`qt_train_bridge.py` 已通过目标 PyTorch 环境的 `py_compile` 语法检查；CMake 重新配置成功；Qt 6.11 / MSVC 2022 完整编译、链接和脚本部署通过，`YOLOAnnotator` 目标达到 100%。未启动真实训练任务，原因是端到端验证需要有效数据集和至少两个 epoch；第一个 epoch 应显示数据点，第二个 epoch 起形成线段。

## 2026-06-18：修复训练日志控制符与指标列错位

- 完成内容：训练子进程日志同时按换行符和回车刷新符拆分；显示前移除 ANSI 终端控制序列及退格符；日志控件改为只接收纯文本，使用系统等宽字体并关闭自动换行；进程结束时补充处理缓冲区中的末尾内容。
- 修改文件：`mainwindow.h`、`mainwindow.cpp`、`PROJECT_LOG.md`。
- 修改原因：Ultralytics 的进度条面向终端输出，包含 `ESC[K` 清行码和 `\r` 原地刷新符；原界面未过滤这些字符，并用比例字体展示依赖空格对齐的指标表，导致出现 `□[K`、异常空白和列对应不齐。
- 修改效果：训练日志不再显示 ANSI 控制码；`Class / Images / Instances / Box(P / R / mAP50 / mAP50-95` 与下一行数值使用等宽字符保持列对齐，连续空格按原样保留。
- 风险与限制：PyTorch 在 Ultralytics `head.py` 中产生的 `__floordiv__ is deprecated` 是当前依赖版本的兼容性警告，本次保留其文本以便排查，不修改第三方包；进度条每次回车刷新仍会形成独立日志行，而不是覆盖上一行。
- 验证：Qt 6.11 / MSVC 2022 已成功编译本次修改并完成可执行文件链接，只有构建后的模型部署步骤失败；失败原因是 `E:\QT\project\2\yolo26n.pt` 当前不存在，CMake 无法将其复制到输出目录。未重新执行真实训练界面验证，需恢复该模型文件后重新构建并启动一次短训练确认显示效果。

## 2026-06-18：默认训练模型改为 yolo26n.pt

- 完成内容：将训练页和 Python 训练桥接的默认模型改为工作区现有的 `yolo26n.pt`；构建后自动复制模型到可执行文件目录；把旧默认值 `yolov8n.pt` 自动迁移为新模型绝对路径。
- 修改文件：`mainwindow.cpp`、`qt_train_bridge.py`、`CMakeLists.txt`、`README.md`、`PROJECT_LOG.md`。
- 修改原因：项目已提供 `yolo26n.pt`，继续默认加载 `yolov8n.pt` 会触发额外下载或找不到模型。
- 修改效果：新安装和仍使用旧默认值的环境会直接加载随程序部署的 `yolo26n.pt`；用户主动设置的其他模型路径保持不变。
- 风险与限制：用户描述的文件名为 `yolov26n.pt`，但工作区实际文件名是 `yolo26n.pt`，本次按实际存在文件配置。
- 验证：CMake 重新配置成功，Qt 6.11 / MSVC 2022 构建通过；源模型与可执行目录副本大小均为 5,544,453 字节。

## 2026-06-18：修复单击误建框与重做失效

- 完成内容：创建标注前先检查真实拖拽尺寸，单击不再被最小尺寸约束扩成标注框；撤销/重做回放期间禁止界面联动写入新命令；标注页增加独立撤销、重做按钮及可用状态联动。
- 修改文件：`canvaswidget.h/.cpp`、`mainwindow.cpp`、`README.md`、`PROJECT_LOG.md`。
- 修改原因：零尺寸点击会被 `boundedRect()` 扩为 4×4 框；回放快照时类别/选择信号可能产生新命令并清空 redo 历史。
- 修改效果：单击画布只取消选择或准备操作，不创建框；撤销后可通过按钮、菜单或 `Ctrl+Y` 恢复操作。
- 风险与限制：撤销栈仍按单张图片管理，切换图片会清空上一张图的撤销/重做历史，这是当前设计行为。
- 验证：Qt 6.11 / MSVC 2022 重新构建通过，`YOLOAnnotator` 目标完成 100%；未执行自动化鼠标交互测试，建议人工验证单击、拖框、撤销、重做四步流程。

## 2026-06-18：主窗口工作区拆分与菜单整理

- 完成内容：将图片标注、模型训练、量化与推理拆分为三个独立页面，通过“工作区”菜单切换；移除与“文件”菜单重复的第二行工具栏。
- 修改文件：`mainwindow.h/.cpp`、`quantdialog.h/.cpp`、`README.md`、`PROJECT_LOG.md`。
- 修改原因：原界面将标注、训练和量化推理控件同时堆叠，菜单与工具栏存在重复入口，界面层级混乱。
- 修改效果：主窗口只保留“文件 / 编辑 / 工作区”菜单；标注页采用标注详情、画布、图片列表三栏布局，训练与量化推理各自占用完整页面。
- 风险与限制：训练或推理任务运行时切换页面不会停止任务；关闭主程序仍会按原逻辑终止正在运行的子进程。
- 验证：重新运行 CMake 配置和 Qt 6.11 / MSVC 2022 构建，`YOLOAnnotator` 目标 100% 编译链接成功；使用 Qt offscreen 平台启动 3 秒，程序未异常退出。

## 2026-06-18：补充使用流程与说明

- 完成内容：新增完整用户手册，说明启动、标注、类别管理、数据集结构、训练、量化、Python 推理、ONNX Runtime C++ 推理、数据库和常见问题。
- 修改文件：新增 `README.md`，更新 `PROJECT_LOG.md`。
- 修改原因：原项目缺少可交付给使用者的端到端操作说明和环境配置指引。
- 修改效果：使用者可按推荐流程从图片标注一直操作到训练、模型导出和双后端推理。
- 风险与限制：ONNX Runtime SDK 安装路径和实际模型兼容性仍由用户环境决定；说明中已列出当前输出格式和 float16 输入限制。
- 验证：人工核对说明内容与当前界面字段、快捷键、CMake 参数、数据库表和程序行为一致；本次仅修改 Markdown 文档，无需重新构建。

## 2026-06-18：标注、训练、量化与推理一体化完善

### 完成内容

- 标注画布增加八方向缩放、图片边界约束、最小框尺寸、稳定选择状态、类别修改，以及创建、删除、移动、缩放、改类别的撤销/重做。
- 修复移动框首次跳动、空标注无法清除旧标签、非法 YOLO 标签未校验、缩放范围无限制等问题。
- 类别使用稳定 ID；删除类别时不重排其他 ID，当前图片仍引用类别时禁止删除。
- 增加图片列表、标注详情双向选择、数据集已标注/未标注统计和状态栏。
- 训练界面分别设置训练集与验证集，支持常见 `images/train`、`labels/train` 结构并生成本次训练 YAML。
- 增加模型、设备、图片尺寸、学习率、优化器、epochs、batch、Python、Ultralytics 源码和输出目录设置。
- 训练桥接改为逐行 JSON 协议；界面实时显示 epoch、指标表格和曲线，并将训练任务、指标及模型产物写入 SQLite。
- 量化/推理桥接改为逐行 JSON，增加 FP32、FP16、INT8 参数冲突与校准集校验。
- 推理支持单图或目录、置信度和 IoU 设置、Python/C++ 后端切换、结果翻页、当前任务检测明细和类别统计。
- 增加 ONNX Runtime C++ 推理引擎，包含图像预处理、YOLO 输出解析、NMS、结果绘制和检测框入库。
- 移除源码中的机器专用绝对路径；运行设置通过界面和 `QSettings` 保存。
- CMake 增加可选 `ONNXRUNTIME_ROOT`，未安装 ONNX Runtime 时仍可构建，选择 C++ 推理会显示配置提示；构建后自动复制 Python 桥接脚本。

### 涉及文件与原因

- `canvaswidget.h/.cpp`：重构标注交互和撤销栈，清理原乱码实现。
- `mainwindow.h/.cpp`：整合数据集管理、训练配置、JSON 指标、图表、数据库、菜单和状态栏。
- `quantdialog.h/.cpp`：整合量化校验、双推理后端、任务级数据库与结果查看。
- `onnxinferenceengine.h/.cpp`：新增可选 ONNX Runtime 原生推理实现。
- `yoloutils.cpp`：允许保存空标签并校验、裁剪 YOLO 标注。
- `qt_train_bridge.py`、`qt_quant_infer_bridge.py`：取消硬编码路径，输出稳定 JSON 事件。
- `CMakeLists.txt`：加入新模块、可选 ONNX Runtime 配置和脚本部署。

### 效果

- 标注修改可恢复，标签保存更可靠，类别 ID 不再因删除操作被静默重排。
- 训练配置、过程、指标、模型文件与数据库形成闭环。
- Python 推理保留为兼容方案；配置 ONNX Runtime 后可执行真正的 C++ ONNX 推理。
- 项目不再绑定原开发机的 Python、YOLO 源码和磁盘路径。

### 风险、限制与后续待办

- 当前机器未提供 ONNX Runtime SDK，因此只验证了未启用该依赖的降级构建；配置 SDK 后仍需用实际 FP32、FP16、INT8 模型分别做结果对比。
- C++ 后端按 Ultralytics 常见原始 YOLO 检测输出解析；带端到端 NMS 或自定义输出节点的 ONNX 模型需要增加对应适配器。
- FP16 ONNX 如果把输入张量类型也改为 float16，当前 C++ 预处理仍需增加 float16 输入张量转换；权重量化但保持 float 输入的模型不受影响。
- 训练和推理端到端验证需要有效数据集、Ultralytics 环境和模型，本次未启动耗时训练任务。
- 数据集统计目前按同名标签文件是否存在判断，不读取全部标签内容做质量评分。

### 验证

- Python AST 语法检查：通过，`qt_train_bridge.py` 与 `qt_quant_infer_bridge.py` 均可解析。
- 硬编码路径扫描：通过，源码中未发现原 `E:/AI_learning`、Anaconda 环境等机器专用路径。
- CMake 配置：通过，Qt 6.11.0 / MSVC 2022，未配置 ONNX Runtime 的降级模式。
- C++ 构建：通过，目标 `YOLOAnnotator` 100% 编译并链接成功。
- 未执行：真实训练、量化、Python 推理、ONNX C++ 推理和交互式 GUI 人工验收；原因是工作区未提供数据集、模型及 ONNX Runtime SDK。

## 2026-06-22：完成项目2报告的调研、证据映射与候选动机阶段

- 本次完成：按 PaperSpine 流程修正报告材料范围，盘点项目源码、数据集、构建产物和100轮训练结果；建立来源映射、目标场景研究、优秀样例学习、技术现状差距图、写作风格配置、30条候选引用库及三种候选写作动机。
- 修改文件或模块：更新 `paper_rewriting_output/paper_spine_config.json`、`paper_spine_config.md`；新增 `source_map.md`、`research_dossier.md`、`exemplar_learning_dossier.md`、`sota_gap_map.md`、`style_profile.md`、`motivation_options_after_research.md`、`citation_support_bank.md`、`citation_quality_audit.md` 和 `reference_materials/source_index.md`。未修改业务代码、原始报告模板、数据集和实验产物。
- 修改原因：报告必须先明确本地证据边界并由用户确认控制性动机，避免将工具集成误写成算法创新，或虚构性能、团队协作、公开仓库和部署结论。
- 修改效果：形成可追溯的“项目事实—外部文献—候选主线”关系；明确第100轮 Precision、Recall、mAP50、mAP50-95 分别为0.88156、0.82313、0.89134、0.67179，并将不同轮次的单项最高值分开记录。
- 风险、限制与待办：GUI三工作区截图仍待用户提供；正文、LaTeX和Word必须等待用户确认动机后才能生成；当前未检测到LaTeX引擎或LibreOffice。
- 验证：`citation_bank_check.py --target-count 10` 通过，候选30条且近年文献24条；`citation_quality_audit.py --write` 完成，26条DOI通过、4条仅含arXiv标识的候选需人工核对，综合质量得分82/100；通过脚本复核 `results.csv` 共100行及数据集划分为训练385张、验证97张。

## 2026-06-22：完成项目2课程报告LaTeX与Word版本

- 本次完成：用户确认“一体化桌面工作流”动机后，完成约8026个中文字符的九章课程报告，摘要280字符；生成系统架构图、工程数据流图，纳入训练曲线、归一化混淆矩阵和验证预测图，并为三张待提供的GUI真实截图保留明确占位。
- 修改文件或模块：在 `paper_rewriting_output/` 新增已确认动机、证据库、图表映射、主张登记、章节蓝图、30行逐段写作矩阵、人性化矩阵、正文中间稿、结构化评审、逻辑迁移审计及最终检查报告；生成 `final_paper/main.tex`、`final_paper/paper.docx` 和 `final_paper/figures/`。更新 PaperSpine 配置与本日志。未修改业务源码、依赖、数据集、实验结果或原始 `项目报告模板-20260620.docx`。
- 修改原因：需要把项目源码与实验材料转换为符合课程模板的完整报告，同时限制未经验证的性能、效率、协作、QAT和部署主张。
- 修改效果：报告形成“工程痛点—一体化架构—模块实施—实验证据—问题边界—后续验证”的统一主线；第100轮指标与不同轮次单项最高值分别呈现，避免拼接为虚假最佳模型。
- 风险、限制与待办：GUI标注页、训练页、量化与推理页截图尚待用户提供；本机无LaTeX引擎，未生成PDF；本机无LibreOffice，DOCX无法逐页渲染验收；双后端一致性、速度和量化效果未进行新实验。
- 验证：`latex_guard.py` 0错误0警告；`word_guard.py` PASS（188段，文本长度12896）；`humanize_check.py` PASS（78行矩阵覆盖77段，覆盖率101%）；`integrity_audit.py` LaTeX gate READY；`artifact_check.py` PASS；引用数量与新近比例检查PASS，引用质量审计26条DOI通过。结构化评审独立性验证未通过（得分0.13），原因是当前规则禁止启动独立子代理，已在评审报告中披露。

## 2026-06-22：新增项目 `.gitignore`

- 本次完成：为当前 Qt/C++ + Python + YOLO 项目新增根目录 `.gitignore`，覆盖 Qt Creator 本地配置、CMake 构建目录、Clangd 缓存、Python 字节码缓存、训练/推理运行产物和本地数据集目录。
- 修改文件或模块：新增 `.gitignore`；更新 `PROJECT_LOG.md`。
- 修改原因：当前仓库根目录尚无忽略规则，Git 状态会混入构建输出、运行数据库、训练结果和本地环境文件，影响后续初始化仓库、查看变更和提交源码。
- 修改效果：后续执行 Git 跟踪时，会优先聚焦源码、文档和需要版本管理的工程文件，减少 `build/`、`runs/`、`.qtcreator/`、`__pycache__/` 以及数据集目录带来的噪音。
- 风险、限制与待办：本次采用偏保守策略，未忽略根目录模型权重 `yolo26n.pt`、`third_party/` SDK、报告文档和 `paper_rewriting_output/`；如果你后面想把仓库收紧为“仅源码/文档”，可以再补充针对大模型文件、第三方依赖和报告产物的规则。
- 验证：只读检查项目目录结构、现有 Git 状态和运行产物位置；确认 `.gitignore` 规则与 `build/`、`runs/`、`.qtcreator/`、`__pycache__/`、`motorcycle_dataset/`、`yolo_motor_dataset/` 等实际目录匹配。本次未执行 `git status` 最终复核安全目录配置外的全局写入操作。

## 2026-06-22：修复 paper.docx 第四部分中文被写成问号的问题

- 本次完成：重新将 `paper.docx` 第四部分“方法与技术”写回为中文正文，并一并修复该部分第一张架构表中被错误写成问号的中文单元格。
- 修改文件或模块：`paper.docx`、`PROJECT_LOG.md`，新增辅助脚本 `fix_paper_section4.py` 用于以 UTF-8 方式稳定写回 DOCX。
- 修改原因：此前通过终端内联脚本直接写入 Word 文档时受到控制台编码影响，导致第四部分正文与表格中的中文内容被实际写成 `?`，不是单纯显示乱码。
- 修改效果：`paper.docx` 中“四、方法与技术”“4.1 技术选型”“4.2 系统设计”“4.3 开发流程”以及对应正文均已恢复为中文；图1、图2、原有图表对象和整体文档结构保留不变；第一张架构表恢复为中文表头与中文职责说明。
- 风险、限制与后续：本次修复聚焦第四部分，不改动第五部分及后续章节；由于本机未检测到 `soffice`/LibreOffice，未完成逐页渲染验收，如需进一步检查分页和版式，需要在可渲染环境下继续核验。
- 验证：使用 `python-docx` 重新读取 `paper.docx`，并以 `unicode_escape` 方式核对第 29–55 段，确认正文为真实中文而非问号；同时复核第二张表（第四部分架构表）各单元格内容已恢复中文；确认 `paper.docx` 文件已成功保存并更新时间刷新。

## 2026-06-22：发布项目到 GitHub

- 本次完成：整理并发布当前 `main` 分支到 GitHub 仓库 `bingbing-guo/LabelYolo`。
- 修改文件或模块：仅更新 `PROJECT_LOG.md` 以记录发布操作；业务源码、依赖版本、模型和构建配置均未修改。
- 修改原因：为项目建立可访问的远程版本库，便于代码托管、版本追踪和后续协作。
- 修改效果：本地仓库配置 `origin` 后，当前提交历史与发布记录可推送至 GitHub。
- 风险、限制与后续待办：本地数据集、构建目录、训练产物和报告输出仍受 `.gitignore` 保护，不会随本次发布上传；后续开发应在提交前继续检查敏感信息和大文件。
- 验证：Git 工作区初始状态干净；Git 对象完整性检查通过；未发现名称疑似密钥或凭据的已跟踪文件；历史中最大对象约 5.5 MB，低于 GitHub 100 MB 单文件限制。本次仅发布现有代码，未重复执行项目构建或业务测试。
